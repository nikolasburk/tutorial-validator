{
  "name": "tutorial-validator",
  "version": "1.0.0",
  "description": "A CLI tool that validates step-by-step tutorials by executing them",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist && pnpm install"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.9.2",
    "@vitest/ui": "^4.0.7",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.7"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "zod": "^4.1.12"
  }
}
